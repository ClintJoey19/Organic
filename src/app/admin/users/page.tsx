import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Admin | Users",
};

const page = () => {
  return <div>Users</div>;
};

export default page;
