interface StoreProps {
  id: string;
  name: string;
  description: string;
  bannerImg: string;
  totalProducts: number;
}

const Store = () => {
  return <div>Store</div>;
};

export default Store;
